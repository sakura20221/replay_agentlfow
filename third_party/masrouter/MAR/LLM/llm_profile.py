llm_profile = [
                {'Name': 'qwen3-8b',
                 'Description': 'Qwen3-8B, an 8-billion-parameter hybrid-reasoning                    model from the Qwen3 family, served locally by vLLM behind the                    experiment proxy. Thinking mode is disabled and temperature is                    fixed at 0 for every call, so replies are direct answers.                    Running locally, the model has no per-token monetary price.'},
                ]
